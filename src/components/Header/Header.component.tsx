import { AppShell } from '@mantine/core';

const Header = () => {
  return (
    <AppShell header={{ height: '6rem' }} padding='lg'>
      <AppShell.Header p={'lg'}>
        <p>Manufac Task</p>
      </AppShell.Header>
    </AppShell>
  );
};

export default Header;
